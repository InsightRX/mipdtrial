# Core function to calculate the sampling time for a row in a sampling schema data.frame.

Core function to calculate the sampling time for a row in a sampling
schema data.frame.

## Usage

``` r
get_sampling_time_core(row, regimen)
```

## Arguments

- row:

  a single row from a sampling_time_design data.frame

- regimen:

  PKPDsim regimen
