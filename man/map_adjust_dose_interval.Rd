% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_adjust_dose_interval.R
\name{map_adjust_dose_interval}
\alias{map_adjust_dose_interval}
\title{Adjust both dose and interval to achieve a target metric using MAP Bayesian
estimation}
\usage{
map_adjust_dose_interval(
  tdms,
  est_model,
  parameters,
  omega,
  ruv,
  regimen,
  covariates = NULL,
  target_design,
  dose_update,
  grid = NULL,
  interval_grid = c(6, 8, 12, 24, 48, 72),
  preferred_interval = 24,
  attainment_tolerance = 0.05,
  settings = NULL,
  ...
)
}
\arguments{
\item{tdms}{observations to be included in MAP estimates}

\item{est_model}{model used for estimation}

\item{parameters}{population parameters of estimation model}

\item{omega}{omega matrix of estimation model}

\item{ruv}{residual error distribution of estimation model, a named list with
elements for \code{prop} (proportional) and \code{add} (additive).}

\item{regimen}{PKPDsim regimen object}

\item{covariates}{named list of PKPDsim covariates}

\item{target_design}{object specifying target design, created using function
\code{create_target_design()}}

\item{dose_update}{update dose from which dose?}

\item{grid}{vector specifying doses or intervals to use as test grid,
Example: \code{seq(from = 50, to = 500, by = (500 - 50) / 10)}}

\item{interval_grid}{numeric vector of candidate intervals (hours) to
evaluate. Default: \code{c(6, 8, 12, 24, 48, 72)}.}

\item{preferred_interval}{numeric scalar indicating the preferred interval
(hours) for tie-breaking when multiple (dose, interval) pairs achieve
similar attainment. Default: \code{24}.}

\item{attainment_tolerance}{numeric scalar (fraction of target value)
defining the tolerance band for tie-breaking. Candidates within
\code{attainment_tolerance * abs(target_value)} of the best attainment are
considered equivalent. Default: \code{0.05}.}

\item{settings}{list of arguments to be used in dose update, e.g.
\code{dose_resolution}, \code{min_dose}, \code{max_dose}.}

\item{...}{arguments passed on to PKPDsim::sim via dose grid search}
}
\value{
Returns a named list: \code{regimen}: the updated regimen;
\code{dose_update}: the dose number updated; \code{new_dose}: the selected dose;
\code{new_interval}: the selected interval; \code{additional_info}: the MAP
parameter estimates; \code{gof}: goodness-of-fit data.frame.
}
\description{
Given a set of levels and a model definition, performs MAP Bayesian
estimation of individual PK/PD parameters, then searches over a grid of
candidate intervals. For each interval, the optimal dose is found via
\code{dose_grid_search()}. The best (dose, interval) pair is selected based on
distance to target, with tie-breaking toward a preferred interval.
}
